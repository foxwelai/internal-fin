# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
# Any PostgreSQL 14+ instance. The value below matches `npm run db:start`,
# which boots a throwaway local server into ./.devdb with no system install.
# In production, point this at your managed Postgres and add ?sslmode=require.
DATABASE_URL="postgresql://foxwel:foxwel@localhost:55432/foxwel_finance"

# Only needed when the host above is a connection pooler that cannot run
# migrations — Neon, Supabase, PgBouncer. Point this at the direct endpoint.
# On Neon that is the same host with the "-pooler" suffix removed.
# Leave unset for a plain PostgreSQL instance.
DIRECT_URL=""

# ---------------------------------------------------------------------------
# Authentication (Clerk)
# ---------------------------------------------------------------------------
# From https://dashboard.clerk.com -> your app -> API keys, or run:
#   npx clerk@latest env pull --app <app_id> --file .env
# Set the same two values in Vercel -> Project Settings -> Environment Variables.
# Never commit real values.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=""
CLERK_SECRET_KEY=""

# ---------------------------------------------------------------------------
# Asset bills (Cloudinary)
# ---------------------------------------------------------------------------
# From https://console.cloudinary.com -> Settings -> API Keys. Either paste the
# single "API environment variable" URL:
#   CLOUDINARY_URL="cloudinary://<api_key>:<api_secret>@<cloud_name>"
# or fill in the three values below. Bills are uploaded as private
# ("authenticated") files and only served to approved users through the app.
# Leave all of them empty to store bills in the database instead.
# Add the same values in Vercel -> Project Settings -> Environment Variables.
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
# Optional. Folder inside your Cloudinary media library.
CLOUDINARY_FOLDER="foxwel-finance/asset-bills"

# ---------------------------------------------------------------------------
# First super admin, approved by `npm run db:seed`
# ---------------------------------------------------------------------------
# Only used when no super admin exists yet. That person gets in the first time
# they sign in to Clerk with this email, once Clerk has verified it. Everyone
# after that is approved from Settings -> Team. Not needed on Vercel.
SUPER_ADMIN_EMAIL="admin@example.com"
SUPER_ADMIN_NAME="Super Admin"

# ---------------------------------------------------------------------------
# Demo data
# ---------------------------------------------------------------------------
# `npm run db:seed:demo` loads a clearly-labelled fictional dataset.
# It is never loaded automatically and can be removed from Settings.
